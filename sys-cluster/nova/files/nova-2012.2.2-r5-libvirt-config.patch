diff -Naur nova-2012.2.2.orig/nova/virt/libvirt/driver.py nova-2012.2.2/nova/virt/libvirt/driver.py
--- nova-2012.2.2.orig/nova/virt/libvirt/driver.py	2013-09-13 10:18:51.806551722 +0200
+++ nova-2012.2.2/nova/virt/libvirt/driver.py	2013-09-16 13:39:15.139142560 +0200
@@ -156,6 +156,11 @@
                help='Override the default disk prefix for the devices attached'
                     ' to a server, which is dependent on libvirt_type. '
                     '(valid options are: sd, xvd, uvd, vd)'),
+    cfg.StrOpt('libvirt_disk_iomode',
+               default=None,
+               help='Override the default disk io mode for the devices attached'
+                    ' to a server. '
+                    '(valid options are: native, threads)'),
     cfg.IntOpt('libvirt_wait_soft_reboot_seconds',
                default=120,
                help='Number of seconds to wait for instance to shut down after'
@@ -1630,6 +1635,7 @@
                                        root_device,
                                        bus,
                                        root_device_type)
+                    diskos.driver_io = FLAGS.libvirt_disk_iomode
                     devices.append(diskos)
 
                 ephemeral_device = None
diff -Naur nova-2012.2.2.orig/nova/virt/libvirt/config.py nova-2012.2.2/nova/virt/libvirt/config.py
--- nova-2012.2.2.orig/nova/virt/libvirt/config.py	2013-09-13 10:18:51.806551722 +0200
+++ nova-2012.2.2/nova/virt/libvirt/config.py	2013-09-16 13:39:15.139142560 +0200
@@ -354,6 +354,7 @@
         self.driver_name = None
         self.driver_format = None
         self.driver_cache = None
+        self.driver_io = None
         self.source_path = None
         self.source_protocol = None
         self.source_host = None
@@ -380,6 +381,8 @@
                 drv.set("type", self.driver_format)
             if self.driver_cache is not None:
                 drv.set("cache", self.driver_cache)
+            if self.driver_io is not None:
+                drv.set("io", self.driver_io)
             dev.append(drv)
 
         if self.source_type == "file":
